import { HeroSlide } from '../components';
import { Movies } from '../components/Movies';

export const HomePage = () => {
  return (
    <div>
      <HeroSlide />
      <Movies />
    </div>
  );
};
