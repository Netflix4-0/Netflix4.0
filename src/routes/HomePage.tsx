import { HeroSlide } from '../components/HeroSlide';
import { Movies } from '../components/Movies';

export const HomePage = () => {
  return (
    <div>
      <HeroSlide />
      <Movies />
    </div>
  );
};
