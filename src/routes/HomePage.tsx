import { HeroSlide } from '../components';
import { Movies } from '../components/movies/Movies';

export const HomePage = () => {
  return (
    <div>
      <HeroSlide />
      <Movies />
    </div>
  );
};
